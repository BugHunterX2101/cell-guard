# Python
__pycache__/
*.pyc
.venv/
venv/

# AWS SAM
.aws-sam/
samconfig.toml

# Scratch CLI-argument files from agent-frontend/README.md's S3+CloudFront
# first-time setup steps (oac-config.json, dist-config.json, bucket-policy.json) —
# disposable inputs to one-off `aws` commands, not project source.
agent-frontend/oac-config.json
agent-frontend/dist-config.json
agent-frontend/bucket-policy.json

# Generated by scripts/prepare_policies.py — staged copy of policies/ for
# the gateway Lambda's package; policies/ itself is the edited source.
gateway-policy/src/gateway/policies/

# Node (agent-frontend side)
node_modules/
dist/
build/
.next/

# Env / secrets
.env
.env.*
!.env.example
*.pem

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
